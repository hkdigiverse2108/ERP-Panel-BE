import express from "express";

const router = express.Router();

// router.get("/all", stockVerificationController.getAllStockAdjustment);
// router.post("/add", stockVerificationController.addStockAdjustment);
// router.get("/:id", stockVerificationController.getStockAdjustmentById);
// router.delete("/:id", stockVerificationController.deleteStockAdjustmentById);

export const stockVerificationRouter = router;
